apiVersion: apps/v1
kind: Deployment
metadata:
  name: zookeeper
  namespace: bienvenue-k8s
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zookeeper
  template:
    metadata:
      labels:
        app: zookeeper
    spec:
      containers:
        - name: zookeeper
          image: zookeeper:3.9.5
          ports:
            - containerPort: 2181